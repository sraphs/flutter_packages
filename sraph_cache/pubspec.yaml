name: sraph_cache
description: Flutter package for reading and writing simple key-value pairs.
version: 1.0.1
homepage: "https://github.com/sraphs/flutter_packages"

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15
  synchronized: ^3.0.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.2.0
